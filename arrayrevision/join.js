//join is used to join the elements of the array to convert them into the
//string.



let arr=['mahesh', 'is', 'best'];
let str=arr.join('');
let str2=arr.join(' ');
let str3=arr.join('-');
console.log(str);
console.log(str2);
console.log(str3);